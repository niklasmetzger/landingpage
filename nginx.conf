server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Große Download-Dateien (DMG/MSI) erlauben
    client_max_body_size 300M;

    location / {
        try_files $uri $uri/ =404;
    }

    # JSON-Listing des Downloads-Ordners – die Landingpage liest darüber automatisch,
    # welche Mac-/Windows-Datei aktuell im Ordner liegt (Erkennung per Endung).
    location = /assets/downloads/ {
        autoindex on;
        autoindex_format json;
    }

    # Downloads als Datei ausliefern, nicht im Browser öffnen
    location /assets/downloads/ {
        types { }
        default_type application/octet-stream;
        add_header Content-Disposition "attachment";
    }

    # Statische Assets cachen
    location ~* \.(css|js|svg|png|jpg|jpeg|webp|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, max-age=604800";
    }
}
